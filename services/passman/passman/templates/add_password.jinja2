{% extends "layout.jinja2" %}

{% block content %}
    <div class="row mt-3 justify-content-center">
        <div class="col justify-content-center">
            <form class="form" action="/add_password/" method="post">
                <div class="form-row justify-content-center">
                    <div class="col">
                        <h4 class="text-center">Add password to your storage</h4>
                    </div>
                </div>
                <div class="form-row justify-content-center">
                    <div class="form-group col-8">
                        <label for="username-input">Password</label>
                        <input type="text" class="form-control {{ 'is-invalid' if error else '' }}" name="password"
                               id="password-input">
                        <div class="invalid-feedback">
                            {{ error }}
                        </div>
                    </div>
                </div>
                <div class="form-row justify-content-center">
                    <button type="submit" class="btn btn-primary">Submit</button>
                </div>
            </form>
        </div>
    </div>

{% endblock content %}
